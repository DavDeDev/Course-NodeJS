const names = require('./3.1 - names');
const sayHi = require('./3.2 - utils');
const data = require('./4 - alternative export');

//console.log(data);

//Even if we don't assign the function to a variable, it will still run
require('./5 - mind grenade');


//Node.js has built-in modules, we can access them without installing anything. They are like functions in different files that we can use right away.